# Whitboard Environment Configuration
# Copy this file to .env and update the values

# =============================================================================
# Public URL Configuration
# =============================================================================

# The public URL where the application is accessible (used by Makefile and app)
PUBLIC_URL=https://draw.example.com

# Local port for Docker to expose Caddy (default: 8088)
LOCAL_PORT=8088

# =============================================================================
# Authentication (Required)
# =============================================================================

EXCALIDRAW_USER=your_username
# Generate with: docker run --rm caddy:2 caddy hash-password --plaintext 'your_password'
# IMPORTANT: Escape $ signs by doubling them ($ -> $$)
EXCALIDRAW_PASSWORD_BASE64=$$2a$$14$$your_hash_here
